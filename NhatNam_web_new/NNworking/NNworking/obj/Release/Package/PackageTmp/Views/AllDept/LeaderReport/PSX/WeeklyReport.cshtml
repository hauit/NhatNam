
@{
    ViewBag.Title = "Báo cáo tuần";
}

@(Html.DevExtreme().DataGrid<NNworking.Models.C222_LeaderWeeklyInput>()
    .DataSource(ds => ds.WebApi()
        .RouteName("C222_LeaderWeeklyInputApi")
        .LoadAction("GetPSX")
        .InsertAction("Post")
        .UpdateAction("Put")
        .DeleteAction("Delete")
        .Key("ID")
    )
    .ID("dxLeaderWeeklyInput")
    //.Export(e => e.Enabled(true))
    .Paging(page => page.PageSize(10))
    .Pager(pager => pager.ShowPageSizeSelector(true)
        .ShowInfo(true)
        .AllowedPageSizes(new List<int> { 20, 30, 50, 100, 200 })
    )
    .Sorting(sort => sort.Mode(GridSortingMode.Multiple)
        .DescendingText("Date")
    )
    .ColumnAutoWidth(true)
    .FilterRow(fil => fil.Visible(true))
    .FilterPanel(fi => fi.Visible(true))
    .SearchPanel(search => search.Visible(true)
        .HighlightSearchText(true)
        .Width(500)
    )
    .Columns(columns => {

        columns.AddFor(m => m.ID).AllowEditing(false);

        columns.AddFor(m => m.Date).AllowEditing(false).Caption("Ngày");

        columns.AddFor(m => m.Week).AllowEditing(false).Caption("Tuần");

        columns.AddFor(m => m.DeptCode).Caption("Mã bộ phận");

        columns.AddFor(m => m.InputStaff).Visible(false).Caption("Người nhập");

        columns.AddFor(m => m.MachineRate).Caption("Công suất máy");

        columns.AddFor(m => m.EmployeeRate).Caption("Công suất người");

        columns.AddFor(m => m.ErrorRate).Caption("Tỉ lệ lỗi");

        columns.AddFor(m => m.CriticalError).Caption("Lỗi nghiêm trọng");
    })
    .Editing(e => e
        //.AllowAdding(true)
        .AllowUpdating(true)
        //.AllowDeleting(true)
        .Mode(GridEditMode.Form)
    )
    .OnInitNewRow("onInitNewRow")
)

<script>
    function onInitNewRow(e) {
        e.data.ID = 0;
    }
</script>