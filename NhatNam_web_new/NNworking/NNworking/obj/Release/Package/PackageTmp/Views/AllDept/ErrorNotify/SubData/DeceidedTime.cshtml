
@{
    ViewBag.Title = "Số lần gây lỗi";
}

@(Html.DevExtreme().DataGrid<NNworking.Models.C242_DeceidedTime>()
    .DataSource(ds => ds.WebApi()
        .RouteName("C242_DeceidedTimeApi")
        .LoadAction("Get")
        .InsertAction("Post")
        .UpdateAction("Put")
        .DeleteAction("Delete")
        .Key("ID")
    )

    .Paging(page => page.PageSize(10))
    .Pager(pager => pager.ShowPageSizeSelector(true)
        .ShowInfo(true)
        .AllowedPageSizes(new List<int> { 10, 20, 30, 50, 100, 200 })
    )
    .Sorting(sort => sort.Mode(GridSortingMode.Multiple)
        .DescendingText("DeceidedTimeIndex")
    )
    //.Export(e => e.Enabled(true).FileName("DeceidedTime").AllowExportSelectedData(true))
    .ColumnAutoWidth(true)
    .FilterRow(fil => fil.Visible(true))
    .FilterPanel(fi => fi.Visible(true))
    .SearchPanel(search => search.Visible(true)
        .HighlightSearchText(true)
        .Width(500)
    )
    .Columns(columns => {

        columns.AddFor(m => m.ID).AllowEditing(false);

        columns.AddFor(m => m.DeceidedTimeIndex).Caption("Mã số");

        columns.AddFor(m => m.Des).Caption("Nội dung");
    })
    .Editing(e => e
        .AllowAdding(true)
        .AllowUpdating(true)
        .AllowDeleting(true)
    )
    .OnInitNewRow("onInitNewRow")
)

<script>
    function onInitNewRow(e){
        e.data.ID = 0;
        e.data.DeceidedTimeIndex = 0;
    }

</script>