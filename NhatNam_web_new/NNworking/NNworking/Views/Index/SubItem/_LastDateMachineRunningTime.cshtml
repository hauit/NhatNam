

@(Html.DevExtreme().Chart()
        .ID("chartMachineRunningTime")
        .CommonSeriesSettings(s => s
            .ArgumentField("MachineID")
            .Type(SeriesType.Bar)
            .IgnoreEmptyPoints(false)
        )
        .Series(s =>
        {
            s.Add().ValueField("Time")
            .BarWidth(1)
            .Name("TG chạy máy");
        })
        .Legend(l => l
            .VerticalAlignment(VerticalEdge.Bottom)
            .HorizontalAlignment(HorizontalAlignment.Center)
            .ItemTextPosition(Position.Top)
        )
        .ValueAxis(a => a
            .Add()
            .Title(t => t.Text("Time"))
            .Position(Position.Right)
        )
        .Title("Thời gian chạy máy ngày hôm trước")
        .OnExporting("onExportingDatagridToExcel").Export(e => e.Enabled(true))
        .Tooltip(t => t
            .Enabled(true)
            .Location(ChartTooltipLocation.Edge)
            .CustomizeTooltip(@<text>
                function(arg) {
                return {
                text: arg.seriesName + " : " + arg.valueText
                };
                }
            </text>)
                )
                .DataSource(Url.Action("GetMachineWTSRunningTime", "AllDept")
                )
)