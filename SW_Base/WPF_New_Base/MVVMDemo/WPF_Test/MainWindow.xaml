<Window x:Class="WPF_Test.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Test.ViewModels" 
        xmlns:local1="clr-namespace:WPF_Test.Base"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <local:ProductsViewModel x:Key="ViewModelP" />
    </Window.Resources>
    <Grid DataContext="{Binding Source={StaticResource ViewModelP}}">
        <!-- Chia Grid thành hai hàng -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DataGrid ItemsSource="{Binding WebUrl}" SelectedItem="{Binding SelectedItem}" AutoGenerateColumns="False" IsReadOnly="False" CanUserAddRows="True" CanUserDeleteRows="True">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="RowEditEnding">
                    <i:InvokeCommandAction Command="{Binding EnterKeyPressedCommand}" CommandParameter="{Binding SelectedItem}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID,UpdateSourceTrigger=LostFocus}" />
                <DataGridTextColumn Header="Controller" Binding="{Binding Controller,UpdateSourceTrigger=LostFocus}" />
                <DataGridTextColumn Header="Action" Binding="{Binding Action,UpdateSourceTrigger=LostFocus}" />
                <DataGridTextColumn Header="LocalPath" Binding="{Binding LocalPath,UpdateSourceTrigger=LostFocus}" />
                <DataGridTextColumn Header="Updated" Binding="{Binding Updated,UpdateSourceTrigger=LostFocus}" />
                <DataGridTextColumn Header="Note" Binding="{Binding Note,UpdateSourceTrigger=LostFocus}" />
            </DataGrid.Columns>
        </DataGrid>
        <!--<ListView ItemsSource="{Binding Products}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Note" DisplayMemberBinding="{Binding Note}" />
                </GridView>
            </ListView.View>
        </ListView>-->
    </Grid>
</Window>
